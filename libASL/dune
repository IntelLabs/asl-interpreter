(ocamllex lexer)

(menhir
 (modules asl_parser)
 (flags --explain))

(library
 (name libASL)
 (public_name asli.libASL)
 (flags
  (:standard -w -27 -cclib -lstdc++))
 (modules
   asl_ast
   asl_fmt
   asl_parser
   asl_utils
   asl_visitor
   backend_c
   backend_verilog
   builtin_idents
   chekhov
   cpu
   elf
   error
   eval
   flags
   format_utils
   global_checks
   ident
   lattice
   lexer
   lexersupport
   loadASL
   obj32
   primops
   tcheck
   utils
   value
   visitor
   xform_bitslices
   xform_bittuples
   xform_case
   xform_constprop
   xform_desugar
   xform_getset
   xform_int_bitslices
   xform_lower
   xform_mono
   xform_rmw
   xform_simplify_expr
   xform_tuples)
 (libraries ocolor yojson zarith z3))
